# Winner Detection Scheduler Configuration
# Agent 02 - Scheduled Winner Check

# Enable/disable the winner detection scheduler
# Default: true (enabled)
ENABLE_WINNER_SCHEDULER=true

# Cron schedule for winner detection
# Default: '0 */6 * * *' (every 6 hours)
# Format: minute hour day month weekday
# Examples:
#   - Every 6 hours: '0 */6 * * *'
#   - Every 4 hours: '0 */4 * * *'
#   - Every day at 2am: '0 2 * * *'
#   - Every hour: '0 * * * *'
WINNER_CHECK_SCHEDULE=0 */6 * * *

# Minimum ROAS (Return on Ad Spend) for winner detection
# Default: 2.0 (2x return)
WINNER_MIN_ROAS=2.0

# Minimum CTR (Click-Through Rate) for winner detection
# Default: 0.02 (2%)
WINNER_MIN_CTR=0.02

# Minimum spend ($) before an ad qualifies as a winner
# Default: 100
WINNER_MIN_SPEND=100

# Minimum hours an ad must run before qualifying
# Default: 24 (1 day)
WINNER_MIN_HOURS=24

# Webhook URL to send notifications when winners are detected
# Optional - leave empty to disable webhook notifications
WINNER_WEBHOOK_URL=https://your-webhook-endpoint.com/winners

# Email address to send winner alerts
# Optional - leave empty to disable email notifications
# Note: Email integration requires additional setup
WINNER_ALERT_EMAIL=alerts@yourcompany.com
